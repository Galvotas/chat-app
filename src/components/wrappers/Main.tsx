import styled from 'styled-components'
import { Colors } from '../../styles/colors'

export const Main = styled.main`
display:flex;
width: 90vw;
height: 90vh;
padding: 1rem;
background-color: ${Colors.Neutral};
border-radius: 1rem;
box-shadow: 1rem 1rem 1rem 1rem ${Colors.NeutralLight};
margin: auto;
`
